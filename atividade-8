#include <stdio.h>
#include <locale.h>

int main () {
	setlocale(LC_ALL,"Portuguese");
	
	int numero, i;
	
	printf("Insira um número: ");
	scanf("%i", &numero);
	printf("Tabuada do número:\n");
	
	for (i = 0; i <= 10; i++){
		printf("%i\n", numero * i);
	}
	
	return 0;
	
}
